#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: Program that prints using the printf() method
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
